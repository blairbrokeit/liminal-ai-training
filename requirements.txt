# This file is kept for users who prefer `pip install -r requirements.txt`.
# The authoritative dependency list is in pyproject.toml.
#
# Recommended install:
#     pip install -e .
#
# For 4-bit quantized loading on CUDA:
#     pip install -e .[quantize]

torch>=2.1.0
transformers>=4.40.0
peft>=0.10.0
trl>=0.8.0
accelerate>=0.27.0

huggingface-hub>=0.23.0
safetensors>=0.4.0

openai>=1.30.0

datasets>=2.19.0

pyyaml>=6.0
python-dotenv>=1.0.0

rich>=13.0.0
python-json-logger>=2.0.0

flask>=3.0.0

# optional, install separately on CUDA hosts:
# bitsandbytes>=0.43.0
